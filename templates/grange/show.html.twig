{% extends 'client.html.twig' %}

{% block title %}Grange{% endblock %}

{% block body %}
    <h1>Grange</h1>

     <div class="col-md-6">
            <h3>Image de la Grange</h3>
            <div class="image-container">
                {% if grange.image %}
                    <img class="img-fluid rounded shadow" src="/uploads/granges/{{ grange.image }}" alt="{{ grange.typeGrange }}">

                {% else %}
                    <img class="img-fluid rounded shadow" src="{{ asset('client/images/grange-placeholder.jpg') }}" alt="Image par défaut">
                {% endif %}
            </div>
        </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grange.id }}</td>
            </tr>
            <tr>
                <th>Type_grange</th>
                <td>{{ grange.typeGrange }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ grange.capacite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_grange_index') }}">Retour</a>

    <a href="{{ path('app_grange_edit', {'id': grange.id}) }}">Modifier</a>

    {{ include('grange/_delete_form.html.twig') }}
{% endblock %}