{% extends 'client.html.twig' %}

{% block title %}Détails de la culture - {{ culture.nom }}{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-success text-center mb-4">{{ culture.nom }}</h1>

        <div class="row">
            <!-- Image de la culture -->
            <div class="col-md-6 text-center mb-4 mb-md-0">
                {% if culture.image %}
                    <img src="{{ asset('uploads/images/' ~ culture.image) }}" alt="{{ culture.nom }}" class="img-fluid rounded-3 shadow-sm" style="max-height: 300px; object-fit: cover;">
                {% else %}
                    <p>Aucune image disponible.</p>
                {% endif %}
            </div>

            <!-- Détails de la culture -->
            <div class="col-md-6">
                <div class="card shadow-sm mb-4">
                    <div class="card-body">
                        <h5 class="card-title">Détails de la culture</h5>
                        <p><strong>Description :</strong> {{ culture.description }}</p>
                        <p><strong>Date de plantation :</strong> {{ culture.datePlantation|date('d/m/Y') }}</p>
                        <p><strong>Date de récolte :</strong> {{ culture.dateRecolte|date('d/m/Y') }}</p>
                        <p><strong>Quantité :</strong> {{ culture.quantite }} KG</p>
                        <p><strong>Catégorie :</strong> {{ culture.categorie }}</p> <!-- Affichage de la catégorie -->
                    </div>
                </div>
            </div>
        </div>

        <!-- Boutons d'action -->
<div class="mt-4 text-center">
    <a href="{{ path('culture_edit', {'id': culture.id}) }}" class="btn btn-warning shadow-sm mr-2">
        <i class="fas fa-edit"></i> Modifier
    </a>
    <form action="{{ path('culture_delete', {'id': culture.id}) }}" method="POST" style="display:inline;" onsubmit="return confirm('Voulez-vous vraiment supprimer cette culture ?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ culture.id) }}">
        <button class="btn btn-danger shadow-sm mr-2" type="submit">
            <i class="fas fa-trash-alt"></i> Supprimer
        </button>
    </form>
    <a href="{{ path('app_culture_index') }}" class="btn btn-secondary shadow-sm mr-2">
        <i class="fas fa-arrow-left"></i> Retour à la liste
    </a>
    <a href="{{ path('culture_pdf', {'id': culture.id}) }}" class="btn btn-primary mr-2">
        <i class="fas fa-file-pdf"></i> Télécharger en PDF
    </a>
</div>

    </div>

    <style>
        body {
            background-color: #f4f7fa;
        }

        h1 {
            font-size: 2.5rem;
            font-weight: 700;
            color: #28a745;
        }

        .card {
            border-radius: 12px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
        }

        .card-body {
            font-size: 1.1rem;
        }

        .card-title {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 20px;
        }

        .btn {
            font-size: 1rem;
            padding: 10px 20px;
            border-radius: 8px;
            transition: transform 0.2s ease;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .btn-primary {
            background-color: #007bff;
            border: none;
        }

        .btn-primary:hover {
            background-color: #0056b3;
        }

        .btn-warning {
            background-color: #ffc107;
            border: none;
        }

        .btn-warning:hover {
            background-color: #e0a800;
        }

        .btn-danger {
            background-color: #dc3545;
            border: none;
        }

        .btn-danger:hover {
            background-color: #c82333;
        }

        .btn-secondary {
            background-color: #6c757d;
            border: none;
        }

        .btn-secondary:hover {
            background-color: #5a6268;
        }

        .text-center {
            text-align: center;
        }

        .me-3 {
            margin-right: 10px;
        }

        .shadow-sm {
            box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.15);
        }
    </style>
{% endblock %}
